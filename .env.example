# @trust-escrow/storage-lib – .env.example
# Copy this to `.env` and fill real values. Do NOT commit `.env`.

# ---- Storage backends ----
# Arweave/Bundlr uploader endpoint that accepts uploads (swap for your provider)
ARWEAVE_ENDPOINT=https://arweave.net
# Optional auth if your uploader requires it (leave blank if not used)
ARWEAVE_API_KEY=

# IPFS API base URL (library will call /api/v0 routes as needed)
IPFS_ENDPOINT=https://ipfs.infura.io:5001
# Pinning token (often 'Bearer <token>')
IPFS_TOKEN=Bearer <your_ipfs_token>

# ---- Public gateways for reads/HEAD ----
IPFS_PUBLIC_GATEWAY=https://ipfs.io/ipfs/
ARWEAVE_PUBLIC_GATEWAY=https://arweave.net/

# ---- PostgreSQL (optional but recommended for audits) ----
# Format: postgresql://user:pass@host:5432/dbname
DATABASE_URL=postgresql://user:pass@host:5432/dbname
