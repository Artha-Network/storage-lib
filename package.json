{
  "name": "@trust-escrow/storage-lib",
  "version": "0.1.0",
  "description": "Thin abstraction over Arweave and IPFS for evidence & rationale storage.",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest"
  },
  "keywords": [
    "arweave",
    "ipfs",
    "storage",
    "escrow"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
